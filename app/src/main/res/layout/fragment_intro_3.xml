<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/GeneralLayoutTheme"
    android:background="@color/colorIntro3">

    <TextView
        android:id="@+id/tvTutorialTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:padding="20dp"
        android:gravity="center"
        android:textColor="@color/colorTextWhite"
        android:textSize="24sp"
        android:text="@string/text.slide3.title"/>

    <ImageView
        android:id="@+id/imTutorial"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_below="@+id/tvTutorialTitle"
        android:contentDescription="@string/text.desc"
        android:src="@drawable/ic_slide3"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/imTutorial"
        android:gravity="center"
        android:padding="15dp"
        android:textSize="16sp"
        android:textColor="@color/colorTextWhite"
        android:text="@string/text.slide3.description"/>

</RelativeLayout>
